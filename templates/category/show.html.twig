{% extends 'base.html.twig' %}

{% block title %}{{ category.nom }}{% endblock %}

{% block body %}
<div class="container my-5">
    <h2 class="text-center">🛍 Produits de la catégorie {{ category.nom }}</h2>
    <div class="row">
        {% for product in products %}
            <div class="col-md-4 my-3">
                <div class="card">
                    <img src="{{ asset('images/' ~ product.image) }}" class="card-img-top" alt="{{ product.nom }}">
                    <div class="card-body text-center">
                        <h5 class="card-title">{{ product.nom }}</h5>
                        <p class="card-text">{{ product.description }}</p>
                        <p class="fw-bold">{{ product.prix }}€</p>
                        <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-primary">Voir le Produit</a>
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-center">Aucun produit disponible dans cette catégorie.</p>
        {% endfor %}
    </div>
</div>
{% endblock %}
