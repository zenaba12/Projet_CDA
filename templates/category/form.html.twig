{% extends 'base.html.twig' %}

{% block body %}
<h1>📂 {{ form.vars.value.id ? 'Modifier' : 'Créer' }} une catégorie</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
   
{{ form_end(form) }}

<a href="{{ path('category_index') }}" class="btn btn-secondary">↩ Retour</a>
{% endblock %}
