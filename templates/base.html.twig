<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Site{% endblock %}</title>

    {#  Intégration correcte de Bootstrap CSS AVANT ton propre CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {#  Ton propre fichier CSS après Bootstrap pour ne pas être écrasé #}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body>

    {% include 'header.html.twig' %}  {# Inclusion de l'entête #}

    <main>
        {% block body %}{% endblock %}
    </main>

    {% include 'trust.html.twig' %}
    {% include 'footer.html.twig' %}  {# Inclusion du pied de page #}

    {# ✅ Intégration correcte de Bootstrap JavaScript avant la fermeture de <body> #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


</body>
</html>
